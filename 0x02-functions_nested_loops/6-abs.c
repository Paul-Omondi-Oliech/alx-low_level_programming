#include "main.h"
/**
 * _abs - computes the absolute value of an intager
 * @n: the int to check
 * Return: the absolute value of int
 */
int _abs(int);
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
